If you got error bellow in vim, then you should execute `sudo chown -R vim_username dotfiles && sudo chmod -R 755 dotfiles`. :
`E303`, `Unable to open swap file for "{filename}", recovery impossible`.
